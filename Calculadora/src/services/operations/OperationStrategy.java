package services.operations;

//Este arquivo faz parte do pacote "services.operations", que contém interfaces e classes para operações matemáticas.
public interface OperationStrategy {
    // Este método executa uma operação matemática com dois números.
    double execute(double a, double b);
}
